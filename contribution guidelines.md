# راهنمای همکاری در پروژه‌های مجله شماره سوم Cyber Sutech

این مخزن شامل پروژه‌های مرتبط با **شماره سوم نشریه Cyber Sutech** است.

## ساختار پروژه‌ها

هر تیم یک پروژه مستقل دارد:

- `projects/software/` — تیم نرم‌افزار
- `projects/artificial-intelligence/` — تیم هوش مصنوعی
- `projects/hardware/` — تیم سخت‌افزار
- `projects/network/` — تیم شبکه

هر عضو فقط روی پروژه مربوط به تیم خودش کار می‌کند.

---

## روند کار اعضای تیم

### ۱. دریافت آخرین تغییرات

قبل از شروع کار، مطمئن شوید Branch اصلی به‌روز است:

```bash
git checkout main
git pull
```

### ۲. ساخت Branch

برای هر تغییر، یک Branch جدا بسازید:

```bash
git checkout -b feature/your-change
```

نام Branch بهتر است کوتاه و مرتبط با کاری باشد که انجام می‌دهید.

مثلاً:

```text
feature/add-readme
feature/train-model
feature/update-circuit
fix/network-config
```

### ۳. انجام تغییرات و Commit

پس از انجام کار:

```bash
git add .
git commit -m "feat: add project documentation"
```

پیام Commit باید کوتاه و مشخص باشد و مطابق استاندارد زیر نوشته شود:

| نوع        | کاربرد                          | مثال                               |
| ---------- | ------------------------------- | ---------------------------------- |
| `feat`     | اضافه کردن قابلیت یا بخش جدید   | `feat: add login system`           |
| `fix`      | رفع باگ یا مشکل                 | `fix: resolve connection issue`    |
| `docs`     | تغییر مستندات                   | `docs: update readme`              |
| `refactor` | بازنویسی کد بدون تغییر عملکرد   | `refactor: improve code structure` |
| `test`     | اضافه کردن یا تغییر تست‌ها      | `test: add unit tests`             |
| `chore`    | تغییرات نگهداری و تنظیمات پروژه | `chore: update configs`            |
| `style`    | تغییرات ظاهری و فرمت کد         | `style: format code`               |

**نکات:**

- پیام Commit باید کوتاه و واضح باشد.
- از توضیحات اضافی و طولانی خودداری کنید.
- نوع Commit را متناسب با تغییر انتخاب کنید.



### ۴. Push کردن Branch

```bash
git push -u origin feature/your-change
```

### ۵. ایجاد Pull Request

بعد از Push، در GitHub یک **Pull Request** از Branch خودتان به `main` ایجاد کنید.

در توضیحات PR به‌صورت کوتاه بنویسید:

- چه کاری انجام داده‌اید؟
- چه فایل‌هایی تغییر کرده‌اند؟
- آیا نکته‌ای وجود دارد که سرگروه باید بررسی کند؟

---

## وظیفه سرگروه

سرگروه هر تیم مسئول بررسی تغییرات اعضای تیم خودش است. سرگروه‌ها اعضای تیم خودشان را به تیم اضافه می‌کنند و دسترسی Maintainer دارند.

روند کار:

```text
عضو تیم
   ↓
Branch
   ↓
Pull Request
   ↓
بررسی توسط سرگروه
   ↓
Approve
   ↓
Merge به main
```

سرگروه قبل از Merge باید بررسی کند که:

- تغییرات مرتبط با پروژه تیم باشد.
- کد یا فایل‌ها ساختار مناسبی داشته باشند.
- تغییرات باعث خراب شدن بخش‌های قبلی نشوند.
- مستندات لازم به‌روز شده باشند.

در صورت وجود مشکل، سرگروه می‌تواند در Pull Request درخواست اصلاح کند.

---

## چند نکته مهم

- ❌ مستقیماً روی `main` کار نکنید.
- ❌ تغییرات تیم‌های دیگر را بدون هماهنگی دستکاری نکنید.
- ✅ برای هر تغییر یک Branch جدا بسازید.
- ✅ قبل از Pull Request، تغییرات خود را تست کنید.
- ✅ Commitها را کوتاه و واضح بنویسید.
- ✅ Pull Request را با توضیح مناسب ایجاد کنید.
- ✅ قبل از Merge، تأیید سرگروه را بگیرید.

هدف این ساختار این است که همکاری تیمی ساده باشد و در عین حال نسخه اصلی پروژه (`main`) مرتب و قابل اعتماد باقی بماند.
